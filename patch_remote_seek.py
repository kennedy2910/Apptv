from pathlib import Path
p = Path('/root/apptv/App.tsx')
text = p.read_text(encoding='utf-8')

text = text.replace(
"type LivePlayback = { item: PlaylistItem; offset: number; index: number };\ntype AppMenuSection = 'termos' | 'sobre' | 'colabore';\n",
"type LivePlayback = { item: PlaylistItem; offset: number; index: number };\ntype ExpandedSeekAnchor = {\n  channelIndex: number;\n  playbackIndex: number;\n  baseOffset: number;\n  startedAtMs: number;\n};\ntype AppMenuSection = 'termos' | 'sobre' | 'colabore';\n"
)

text = text.replace(
"  const [hoverPreview, setHoverPreview] = useState<{ channelIndex: number; playback: LivePlayback; token: number } | null>(null);\n  const [expandedPlayback, setExpandedPlayback] = useState<{ channelIndex: number; playback: LivePlayback } | null>(null);\n  const [isExpandedInfoVisible, setIsExpandedInfoVisible] = useState(false);\n",
"  const [hoverPreview, setHoverPreview] = useState<{ channelIndex: number; playback: LivePlayback; token: number } | null>(null);\n  const [expandedPlayback, setExpandedPlayback] = useState<{ channelIndex: number; playback: LivePlayback } | null>(null);\n  const [expandedSeekAnchor, setExpandedSeekAnchor] = useState<ExpandedSeekAnchor | null>(null);\n  const [isExpandedInfoVisible, setIsExpandedInfoVisible] = useState(false);\n"
)

text = text.replace(
"  const changeExpandedChannel = useCallback((delta: number) => {\n    if (!expandedPlayback) return;\n    const nextChannelIndex = getWrappedNextChannelIndex(expandedPlayback.channelIndex, delta);\n    const nextChannel = state.channels[nextChannelIndex];\n    if (!nextChannel) return;\n    const nextPlayback = getLivePlaybackState(nextChannel);\n    if (!nextPlayback) return;\n\n    selectChannel(nextChannelIndex);\n    setExpandedPlayback({ channelIndex: nextChannelIndex, playback: nextPlayback });\n  }, [expandedPlayback, getWrappedNextChannelIndex, selectChannel, state.channels]);\n",
"  const changeExpandedChannel = useCallback((delta: number) => {\n    if (!expandedPlayback) return;\n    const nextChannelIndex = getWrappedNextChannelIndex(expandedPlayback.channelIndex, delta);\n    const nextChannel = state.channels[nextChannelIndex];\n    if (!nextChannel) return;\n    const nextPlayback = getLivePlaybackState(nextChannel);\n    if (!nextPlayback) return;\n\n    selectChannel(nextChannelIndex);\n    setExpandedPlayback({ channelIndex: nextChannelIndex, playback: nextPlayback });\n  }, [expandedPlayback, getWrappedNextChannelIndex, selectChannel, state.channels]);\n\n  const clampExpandedOffset = useCallback((item: PlaylistItem, nextOffset: number) => {\n    const duration = Number(item.duration || 0);\n    const maxOffset = duration > 0 ? Math.max(0, duration - 0.25) : 0;\n    return Math.min(Math.max(0, nextOffset), maxOffset);\n  }, []);\n\n  const getExpandedEffectiveOffset = useCallback((anchor: ExpandedSeekAnchor | null, playback: LivePlayback, currentNow: number) => {\n    if (\n      anchor &&\n      expandedPlayback &&\n      anchor.channelIndex === expandedPlayback.channelIndex &&\n      anchor.playbackIndex === playback.index\n    ) {\n      return clampExpandedOffset(\n        playback.item,\n        anchor.baseOffset + Math.max(0, (currentNow - anchor.startedAtMs) / 1000),\n      );\n    }\n\n    return clampExpandedOffset(playback.item, playback.offset);\n  }, [clampExpandedOffset, expandedPlayback]);\n\n  const seekExpandedPlayback = useCallback((deltaSeconds: number) => {\n    if (!expandedPlayback) return;\n    if (expandedPlayback.playback.item.type !== ContentType.VIDEO) return;\n\n    const currentOffset = getExpandedEffectiveOffset(expandedSeekAnchor, expandedPlayback.playback, Date.now());\n    const nextOffset = clampExpandedOffset(expandedPlayback.playback.item, currentOffset + deltaSeconds);\n\n    setExpandedSeekAnchor({\n      channelIndex: expandedPlayback.channelIndex,\n      playbackIndex: expandedPlayback.playback.index,\n      baseOffset: nextOffset,\n      startedAtMs: Date.now(),\n    });\n    showExpandedInfo();\n  }, [clampExpandedOffset, expandedPlayback, expandedSeekAnchor, getExpandedEffectiveOffset, showExpandedInfo]);\n"
)

text = text.replace(
"  useEffect(() => {\n    if (!expandedPlayback) {\n      setIsExpandedInfoVisible(false);\n      if (expandedInfoTimerRef.current) {\n        window.clearTimeout(expandedInfoTimerRef.current);\n        expandedInfoTimerRef.current = null;\n      }\n      return;\n    }\n\n    showExpandedInfo();\n\n    return () => {\n      if (expandedInfoTimerRef.current) {\n        window.clearTimeout(expandedInfoTimerRef.current);\n        expandedInfoTimerRef.current = null;\n      }\n    };\n  }, [expandedPlayback?.channelIndex, expandedPlayback?.playback.index, showExpandedInfo]);\n",
"  useEffect(() => {\n    if (!expandedPlayback) {\n      setExpandedSeekAnchor(null);\n      setIsExpandedInfoVisible(false);\n      if (expandedInfoTimerRef.current) {\n        window.clearTimeout(expandedInfoTimerRef.current);\n        expandedInfoTimerRef.current = null;\n      }\n      return;\n    }\n\n    setExpandedSeekAnchor({\n      channelIndex: expandedPlayback.channelIndex,\n      playbackIndex: expandedPlayback.playback.index,\n      baseOffset: expandedPlayback.playback.offset,\n      startedAtMs: Date.now(),\n    });\n    showExpandedInfo();\n\n    return () => {\n      if (expandedInfoTimerRef.current) {\n        window.clearTimeout(expandedInfoTimerRef.current);\n        expandedInfoTimerRef.current = null;\n      }\n    };\n  }, [expandedPlayback?.channelIndex, expandedPlayback?.playback.index, expandedPlayback?.playback.offset, showExpandedInfo]);\n"
)

text = text.replace(
"    const item = expandedPlayback.playback.item;\n    if ((item.type !== ContentType.VIDEO && item.type !== ContentType.AD) || !Number.isFinite(item.duration) || item.duration <= 0) {\n      return;\n    }\n\n    const autoAdvanceLeadMs = expandedPlayback.playback.item.type === ContentType.VIDEO ? 150 : 0;\n    const remainingMs = Math.max(250, Math.round(Math.max(0, item.duration - expandedPlayback.playback.offset) * 1000) - autoAdvanceLeadMs);\n",
"    const item = expandedPlayback.playback.item;\n    if ((item.type !== ContentType.VIDEO && item.type !== ContentType.AD) || !Number.isFinite(item.duration) || item.duration <= 0) {\n      return;\n    }\n\n    const activeOffset = expandedSeekAnchor &&\n      expandedSeekAnchor.channelIndex === expandedPlayback.channelIndex &&\n      expandedSeekAnchor.playbackIndex === expandedPlayback.playback.index\n        ? expandedSeekAnchor.baseOffset\n        : expandedPlayback.playback.offset;\n\n    const autoAdvanceLeadMs = expandedPlayback.playback.item.type === ContentType.VIDEO ? 150 : 0;\n    const remainingMs = Math.max(250, Math.round(Math.max(0, item.duration - activeOffset) * 1000) - autoAdvanceLeadMs);\n"
)

text = text.replace(
"  }, [advanceExpandedPlayback, clearPlaybackAdvanceTimer, expandedPlayback?.channelIndex, expandedPlayback?.playback.index, expandedPlayback?.playback.offset, expandedPlayback?.playback.item.duration, expandedPlayback?.playback.item.type]);\n",
"  }, [advanceExpandedPlayback, clearPlaybackAdvanceTimer, expandedPlayback?.channelIndex, expandedPlayback?.playback.index, expandedPlayback?.playback.offset, expandedPlayback?.playback.item.duration, expandedPlayback?.playback.item.type, expandedSeekAnchor?.channelIndex, expandedSeekAnchor?.playbackIndex, expandedSeekAnchor?.baseOffset]);\n"
)

text = text.replace(
"  const expandedChannel = expandedPlayback ? state.channels[expandedPlayback.channelIndex] : null;\n  const expandedItem = expandedPlayback?.playback.item;\n  const expandedProgress = getPlaybackProgress(expandedPlayback?.playback.offset || 0, expandedItem?.duration);\n",
"  const expandedChannel = expandedPlayback ? state.channels[expandedPlayback.channelIndex] : null;\n  const expandedItem = expandedPlayback?.playback.item;\n  const expandedPlayerOffset = expandedPlayback\n    ? (\n        expandedSeekAnchor &&\n        expandedSeekAnchor.channelIndex === expandedPlayback.channelIndex &&\n        expandedSeekAnchor.playbackIndex === expandedPlayback.playback.index\n          ? expandedSeekAnchor.baseOffset\n          : expandedPlayback.playback.offset\n      )\n    : 0;\n  const expandedEffectiveOffset = expandedPlayback\n    ? getExpandedEffectiveOffset(expandedSeekAnchor, expandedPlayback.playback, now)\n    : 0;\n  const expandedProgress = getPlaybackProgress(expandedEffectiveOffset, expandedItem?.duration);\n"
)

text = text.replace("offset={expandedPlayback.playback.offset}", "offset={expandedPlayerOffset}")

text = text.replace(
"              <div className=\"absolute left-0 right-0 bottom-0 bg-gradient-to-t from-black/85 to-transparent p-[1.2%]\">\n                <div className=\"max-w-[60%] rounded-[0.8vw] border border-white/20 bg-black/45 px-[1%] py-[0.8%]\">\n                  <div className=\"text-[0.8vw] uppercase tracking-[0.16em] text-sky-300\">Agora</div>\n                  <div className=\"text-[1vw] font-bold truncate\">{getItemLabel(expandedItem)}</div>\n                  {expandedItem.duration > 0 && (\n                    <div className=\"mt-[0.8vh]\">\n                      <div className=\"overflow-hidden rounded-full border border-cyan-300/70 bg-white/10 p-[0.14vh] shadow-[0_0_0_0.08vw_rgba(34,211,238,0.25)] backdrop-blur-[2px]\">\n                        <div className=\"h-[0.42vh] overflow-hidden rounded-full bg-white/15\">\n                          <div\n                            className=\"h-full rounded-full bg-[repeating-linear-gradient(135deg,rgba(34,211,238,0.96)_0,rgba(34,211,238,0.96)_0.38vw,rgba(255,255,255,0.26)_0.38vw,rgba(255,255,255,0.26)_0.58vw)] shadow-[0_0_0.85vw_rgba(34,211,238,0.35)]\"\n                            style={{ width: `${expandedProgress.percent}%` }}\n                          />\n                        </div>\n                      </div>\n                      <div className=\"mt-[0.35vh] text-[0.72vw] text-cyan-50/82\">\n                        {expandedProgress.elapsedLabel} / {expandedProgress.durationLabel}\n                      </div>\n                    </div>\n                  )}\n                </div>\n              </div>",
"              <div className=\"absolute left-0 right-0 bottom-0 bg-gradient-to-t from-black/85 to-transparent p-[1.2%]\">\n                <div className=\"max-w-[72%] rounded-[0.8vw] border border-white/20 bg-black/45 px-[1%] py-[0.8%] pointer-events-auto\">\n                  <div className=\"flex items-center justify-between gap-[1.2vw]\">\n                    <div className=\"min-w-0\">\n                      <div className=\"text-[0.8vw] uppercase tracking-[0.16em] text-sky-300\">Agora</div>\n                      <div className=\"text-[1vw] font-bold truncate\">{getItemLabel(expandedItem)}</div>\n                    </div>\n                    {expandedItem.type === ContentType.VIDEO && (\n                      <div className=\"flex items-center gap-[0.6vw] shrink-0\">\n                        <button\n                          type=\"button\"\n                          onClick={(event) => {\n                            event.stopPropagation();\n                            seekExpandedPlayback(-10);\n                          }}\n                          className=\"rounded-full border border-white/20 bg-black/55 px-[0.9vw] py-[0.45vw] text-[0.82vw] font-bold text-white/90 hover:bg-black/75\"\n                        >\n                          -10s\n                        </button>\n                        <button\n                          type=\"button\"\n                          onClick={(event) => {\n                            event.stopPropagation();\n                            seekExpandedPlayback(10);\n                          }}\n                          className=\"rounded-full border border-white/20 bg-black/55 px-[0.9vw] py-[0.45vw] text-[0.82vw] font-bold text-white/90 hover:bg-black/75\"\n                        >\n                          +10s\n                        </button>\n                      </div>\n                    )}\n                  </div>\n                  {expandedItem.duration > 0 && (\n                    <div className=\"mt-[0.8vh]\">\n                      <div className=\"overflow-hidden rounded-full border border-cyan-300/70 bg-white/10 p-[0.14vh] shadow-[0_0_0_0.08vw_rgba(34,211,238,0.25)] backdrop-blur-[2px]\">\n                        <div className=\"h-[0.42vh] overflow-hidden rounded-full bg-white/15\">\n                          <div\n                            className=\"h-full rounded-full bg-[repeating-linear-gradient(135deg,rgba(34,211,238,0.96)_0,rgba(34,211,238,0.96)_0.38vw,rgba(255,255,255,0.26)_0.38vw,rgba(255,255,255,0.26)_0.58vw)] shadow-[0_0_0.85vw_rgba(34,211,238,0.35)]\"\n                            style={{ width: `${expandedProgress.percent}%` }}\n                          />\n                        </div>\n                      </div>\n                      <div className=\"mt-[0.35vh] text-[0.72vw] text-cyan-50/82\">\n                        {expandedProgress.elapsedLabel} / {expandedProgress.durationLabel}\n                      </div>\n                    </div>\n                  )}\n                </div>\n              </div>"
)

p.write_text(text, encoding='utf-8')
print('patched')
