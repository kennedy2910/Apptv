# ROMs

Coloque suas ROMs aqui (por exemplo, arquivos `.nes`, `.smc`, `.sfc`).

- Esta pasta e util para desenvolvimento local.
- Os binarios de ROMs sao ignorados pelo git por padrao (veja `.gitignore`).
- Use apenas ROMs que voce possui legalmente ou tem direitos de distribuir.
